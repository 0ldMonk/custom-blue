# OTP ships commented out — inert until `enable-ssh-otp` uncomments these
# two lines (and any CIDR exclusion block it appends below them).
#KbdInteractiveAuthentication yes
#AuthenticationMethods publickey,keyboard-interactive
