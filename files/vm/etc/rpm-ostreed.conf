# Explicit even though "stage" is currently rpm-ostree's own compile-time
# default — don't rely on that staying true across rpm-ostree versions.
# Pairs with rpm-ostreed-automatic.timer (enabled in the recipe) and the
# rpm-ostree-reboot-required exporter, which picks up what gets staged here.
[Daemon]
AutomaticUpdatePolicy=stage
