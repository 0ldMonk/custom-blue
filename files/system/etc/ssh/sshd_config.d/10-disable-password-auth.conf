# All machines, key-only SSH. Does NOT touch sshd's enable state.
# PermitRootLogin defaults to prohibit-password already, so root login
# (key-only) works without setting it explicitly here.
PasswordAuthentication no
