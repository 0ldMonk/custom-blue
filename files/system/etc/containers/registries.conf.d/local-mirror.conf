[[registry]]
location = "ghcr.io"

[[registry.mirror]]
# HTTP is safe here: cosign verifies the image against cosign.pub regardless of
# transport, so a tampered mirror response fails the signature check.
location = "ghcr-mirror.lan:7272"
insecure = true
